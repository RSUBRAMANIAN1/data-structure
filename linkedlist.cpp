#include<bits/stdc++.h>
using namespace std;

struct node
{ int data;
   node * next;
   void create(node* n, int data)
   {
   	this.n->next=n;
   	this.data=data;
   }
}
void main()
{
	node* head=NULL;
	
	while(n!=0)
	{
	
	create(head,)

}
